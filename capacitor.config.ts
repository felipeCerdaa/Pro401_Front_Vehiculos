import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Pro401_Front_Vehiculos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
